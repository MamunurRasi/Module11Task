version: '3.8'

services:
  fastapi-app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app   # Mount your app folder for live code changes (optional)
    environment:
      - PYTHONUNBUFFERED=1
